What happens when you initialize a repository? Why do you need to do it?
	Git creates the metadata required for the repository. 
How is the staging area different from the working directory and the repository? Repository contains all commits of files added to staging area, working directory contains any files. What value do you think it offers? It's an intermediate area for grouping related files before commit